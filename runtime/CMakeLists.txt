project(haunt_runtime LANGUAGES C VERSION "0.1.0")

# TODO: Need to figure out how to actually "build" this project given that it's intended to serve as
# an executable for each game. Do I build it as a lib and just have each game link to it? Or do I
# have each game build it?
